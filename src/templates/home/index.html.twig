{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-center mb-4">Upcoming Events</h1>

    <!-- Bouton pour rediriger vers la page de création d'événement -->
    {% if is_granted('ROLE_USER') %}
        <div class="text-center mb-4">
            <a href="{{ path('app_event_create') }}" class="btn btn-primary">Create Event</a>
        </div>
    {% endif %}

    <!-- Formulaire de filtre par intervalle de dates -->
    <form method="get" class="mb-4">
        <div class="row">
            <div class="col-md-5">
                <input type="date" name="start_date" class="form-control" value="{{ app.request.query.get('start_date') }}" placeholder="Start Date">
            </div>
            <div class="col-md-5">
                <input type="date" name="end_date" class="form-control" value="{{ app.request.query.get('end_date') }}" placeholder="End Date">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100">Filter</button>
            </div>
        </div>
    </form>

    <!-- Affichage des événements existants -->
    {% if events is empty %}
        <p class="text-center">There are no upcoming events at the moment.</p>
    {% else %}
        <div class="row">
            {% for event in events %}
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ event.title }}</h5>
                            <p class="card-text">{{ event.description|slice(0, 100) }}...</p>
                            <p class="card-text"><strong>Location:</strong> {{ event.location }}</p>
                            <p class="card-text"><strong>Date:</strong> {{ event.startDate|date('d-m-Y H:i') }}</p>
                            <!-- Affichage du bouton "Edit" et "Delete" uniquement pour le créateur de l'événement -->
                            {% if app.user == event.createdBy %}
                                <a href="{{ path('app_event_edit', { id: event.id }) }}" class="btn btn-outline-secondary">Edit</a>
                                <form method="post" action="{{ path('app_event_delete', { id: event.id }) }}" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this event?');">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}
